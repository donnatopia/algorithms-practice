'use strict';

/**
 * @param {...function} fns
 * @return {function}
 */

const compose = (...fns) => {

}

/**
 * @param {...function} fns
 * @return {function}
 */

const pipe = (...fns) => {

}

module.exports = {compose, pipe};
